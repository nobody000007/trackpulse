trigger:
  branches:
    include:
      - main
  paths:
    include:
      - terraform/**
      - pipelines/terraform-pipeline.yml

parameters:
  - name: environment
    displayName: Environment
    type: string
    default: dev
    values:
      - dev
      - staging
      - prod

variables:
  - group: trackpulse-secrets
  - name: workingDir
    value: $(System.DefaultWorkingDirectory)/terraform/environments/${{ parameters.environment }}
  - name: terraformVersion
    value: "1.7.0"

stages:

# ─── Stage 1: Terraform Plan ──────────────────────────────────────────────────
- stage: Plan
  displayName: "Terraform Plan (${{ parameters.environment }})"
  jobs:
  - job: TerraformPlan
    displayName: "Plan infrastructure changes"
    pool:
      name: local-agent

    steps:
    - task: AzureCLI@2
      displayName: "Terraform Init & Plan"
      inputs:
        azureSubscription: azure-service-connection
        scriptType: bash
        scriptLocation: inlineScript
        workingDirectory: $(workingDir)
        addSpnToEnvironment: true
        inlineScript: |
          # Install Terraform (Windows binary)
          curl -sLo terraform.zip "https://releases.hashicorp.com/terraform/$(terraformVersion)/terraform_$(terraformVersion)_windows_amd64.zip"
          unzip -o terraform.zip
          export PATH="$PWD:$PATH"
          terraform version

          # Export Azure credentials for Terraform
          export ARM_CLIENT_ID=$servicePrincipalId
          export ARM_CLIENT_SECRET=$servicePrincipalKey
          export ARM_TENANT_ID=$tenantId
          export ARM_SUBSCRIPTION_ID=$(az account show --query id -o tsv)

          # Init with remote backend
          terraform init

          # Plan
          terraform plan \
            -var="db_admin_password=$(DB_ADMIN_PASSWORD)" \
            -var="nextauth_secret=$(NEXTAUTH_SECRET)" \
            -var="groq_api_key=$(GROQ_API_KEY)" \
            -var="gmail_user=$(GMAIL_USER)" \
            -var="gmail_app_password=$(GMAIL_APP_PASSWORD)" \
            -out=tfplan

          terraform show -no-color tfplan

    - publish: $(workingDir)/tfplan
      artifact: tfplan-${{ parameters.environment }}
      displayName: "Publish plan artifact"

# ─── Stage 2: Approval Gate ───────────────────────────────────────────────────
- stage: WaitForApproval
  displayName: "Waiting for approval (${{ parameters.environment }})"
  dependsOn: Plan
  condition: succeeded()
  jobs:
  - deployment: ApprovalGate
    displayName: "Manual approval required"
    environment: ${{ parameters.environment }}-approval
    pool:
      name: local-agent
    strategy:
      runOnce:
        deploy:
          steps:
          - bash: echo "Approved — proceeding with Terraform Apply"

# ─── Stage 3: Terraform Apply ─────────────────────────────────────────────────
- stage: Apply
  displayName: "Terraform Apply (${{ parameters.environment }})"
  dependsOn: WaitForApproval
  condition: succeeded()
  jobs:
  - job: TerraformApply
    displayName: "Apply infrastructure changes"
    pool:
      name: local-agent

    steps:
    - download: current
      artifact: tfplan-${{ parameters.environment }}
      displayName: "Download plan artifact"

    - task: AzureCLI@2
      displayName: "Terraform Apply"
      inputs:
        azureSubscription: azure-service-connection
        scriptType: bash
        scriptLocation: inlineScript
        workingDirectory: $(workingDir)
        addSpnToEnvironment: true
        inlineScript: |
          # Install Terraform (Windows binary)
          curl -sLo terraform.zip "https://releases.hashicorp.com/terraform/$(terraformVersion)/terraform_$(terraformVersion)_windows_amd64.zip"
          unzip -o terraform.zip
          export PATH="$PWD:$PATH"

          # Export Azure credentials
          export ARM_CLIENT_ID=$servicePrincipalId
          export ARM_CLIENT_SECRET=$servicePrincipalKey
          export ARM_TENANT_ID=$tenantId
          export ARM_SUBSCRIPTION_ID=$(az account show --query id -o tsv)

          # Init
          terraform init

          # Apply the saved plan
          cp "$(Pipeline.Workspace)/tfplan-${{ parameters.environment }}/tfplan" .
          terraform apply -auto-approve tfplan
