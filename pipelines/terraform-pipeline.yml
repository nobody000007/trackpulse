trigger:
  branches:
    include:
      - main
  paths:
    include:
      - terraform/**
      - pipelines/terraform-pipeline.yml

parameters:
  - name: environment
    displayName: Environment
    type: string
    default: dev
    values:
      - dev
      - staging
      - prod

variables:
  - group: trackpulse-secrets
  - name: workingDir
    value: $(System.DefaultWorkingDirectory)\terraform\environments\${{ parameters.environment }}

stages:

# ─── Stage 1: Plan ────────────────────────────────────────────────────────────
- stage: Plan
  displayName: "Terraform Plan (${{ parameters.environment }})"
  jobs:
  - job: TerraformPlan
    displayName: "Plan"
    pool:
      name: local-agent

    steps:
    - task: AzureCLI@2
      displayName: "Terraform Init & Plan"
      inputs:
        azureSubscription: azure-service-connection
        scriptType: ps
        scriptLocation: inlineScript
        workingDirectory: $(workingDir)
        addSpnToEnvironment: true
        inlineScript: |
          $env:ARM_CLIENT_ID       = $env:servicePrincipalId
          $env:ARM_CLIENT_SECRET   = $env:servicePrincipalKey
          $env:ARM_TENANT_ID       = $env:tenantId
          $env:ARM_SUBSCRIPTION_ID = (az account show --query id -o tsv)

          # Register required providers (no-op if already registered)
          az provider register --namespace Microsoft.Web --wait
          az provider register --namespace Microsoft.ContainerService --wait

          # Clean any stale state before init to guarantee a fresh start
          if (Test-Path ".terraform") { Remove-Item -Recurse -Force ".terraform" }
          if (Test-Path ".terraform.lock.hcl") { Remove-Item -Force ".terraform.lock.hcl" }

          terraform init

          terraform plan `
            -var="db_admin_password=$(DB_ADMIN_PASSWORD)" `
            -var="nextauth_secret=$(NEXTAUTH_SECRET)" `
            -var="groq_api_key=$(GROQ_API_KEY)" `
            -var="gmail_user=$(GMAIL_USER)" `
            -var="app_insights_connection_string=$(APP_INSIGHTS_CONNECTION_STRING)" `
            -var="gmail_app_password=$(GMAIL_APP_PASSWORD)" `
            -out=tfplan

          terraform show -no-color tfplan

    - publish: $(workingDir)/tfplan
      artifact: tfplan-${{ parameters.environment }}
      displayName: "Publish plan artifact"

# ─── Stage 2: Approval ────────────────────────────────────────────────────────
- stage: WaitForApproval
  displayName: "Waiting for approval (${{ parameters.environment }})"
  dependsOn: Plan
  condition: succeeded()
  jobs:
  - deployment: ApprovalGate
    displayName: "Manual approval"
    environment: ${{ parameters.environment }}-approval
    pool:
      name: local-agent
    strategy:
      runOnce:
        deploy:
          steps:
          - script: echo Approved

# ─── Stage 3: Apply ───────────────────────────────────────────────────────────
- stage: Apply
  displayName: "Terraform Apply (${{ parameters.environment }})"
  dependsOn: WaitForApproval
  condition: succeeded()
  jobs:
  - job: TerraformApply
    displayName: "Apply"
    pool:
      name: local-agent

    steps:
    # Disable git clean so .terraform/ and .terraform.lock.hcl from Plan are preserved
    - checkout: self
      clean: false

    - download: current
      artifact: tfplan-${{ parameters.environment }}
      displayName: "Download plan artifact"

    - task: AzureCLI@2
      displayName: "Terraform Apply"
      inputs:
        azureSubscription: azure-service-connection
        scriptType: ps
        scriptLocation: inlineScript
        workingDirectory: $(workingDir)
        addSpnToEnvironment: true
        inlineScript: |
          $env:ARM_CLIENT_ID       = $env:servicePrincipalId
          $env:ARM_CLIENT_SECRET   = $env:servicePrincipalKey
          $env:ARM_TENANT_ID       = $env:tenantId
          $env:ARM_SUBSCRIPTION_ID = (az account show --query id -o tsv)

          $planFile = "$(Pipeline.Workspace)\tfplan-${{ parameters.environment }}\tfplan"
          if (-not (Test-Path $planFile)) {
            Write-Error "Plan file not found at $planFile"
            exit 1
          }
          Copy-Item $planFile -Destination "tfplan"

          terraform apply -auto-approve tfplan
